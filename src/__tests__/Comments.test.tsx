describe('Comments component', () => {
    //renders without crashing
    //doesnt show input field initially
    //shows an icon for comments
    //does show input filed when comment icon is clicked
    //does not show input field when comment icon is clicked twice
    //empties the input field when user presses the enter key
    //empties the input field when user presses send
    //shows message in comment-section when user writes then presses the enter key
    //shows message in comment-section when user writes then presses send
    
})